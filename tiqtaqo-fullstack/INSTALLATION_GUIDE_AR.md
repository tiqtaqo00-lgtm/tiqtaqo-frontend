# 📖 دليل التثبيت الكامل - TiqtaQo Full-Stack

## 🎯 ما الذي تم إنجازه؟

تم تحويل موقعك من نظام بسيط يستخدم localStorage إلى **نظام متكامل Full-Stack** يتضمن:

### ✅ Backend (الخادم الخلفي)
- **Node.js + Express**: خادم API قوي وسريع
- **SQLite Database**: قاعدة بيانات حقيقية لحفظ البيانات بشكل دائم
- **JWT Authentication**: نظام مصادقة آمن
- **File Upload System**: نظام رفع الصور
- **RESTful API**: واجهة برمجية كاملة

### ✅ Frontend (الواجهة الأمامية)
- **تحديث JavaScript**: للاتصال بالـ API بدلاً من localStorage
- **لوحة تحكم محدثة**: مع وظائف كاملة
- **صفحة تسجيل دخول جديدة**: بتصميم عصري

## 📦 محتويات المشروع

```
tiqtaqo-fullstack/
│
├── 📁 backend/                    # الخادم الخلفي
│   ├── server.js                  # الخادم الرئيسي
│   ├── database.js                # إدارة قاعدة البيانات
│   ├── package.json               # المكتبات المطلوبة
│   ├── .env                       # الإعدادات
│   ├── 📁 database/               # قاعدة البيانات
│   └── 📁 uploads/                # الصور المرفوعة
│
├── 📁 frontend/public/            # الواجهة الأمامية
│   ├── index.html                 # الصفحة الرئيسية
│   ├── 📁 js/
│   │   ├── api.js                 # ⭐ جديد - وظائف API
│   │   ├── main-updated.js        # ⭐ محدث
│   │   └── main.js                # القديم (للمرجع)
│   ├── 📁 admin/
│   │   ├── login-updated.html     # ⭐ محدث
│   │   ├── admin-updated.js       # ⭐ محدث
│   │   ├── dashboard.html
│   │   ├── login.html             # القديم
│   │   └── admin.js               # القديم
│   └── ...
│
├── README.md                      # التوثيق الكامل
├── QUICK_START_AR.md              # دليل البدء السريع
└── INSTALLATION_GUIDE_AR.md       # هذا الملف
```

## 🚀 خطوات التثبيت التفصيلية

### الخطوة 1: فك الضغط
```bash
unzip tiqtaqo-fullstack-final.zip
cd tiqtaqo-fullstack
```

### الخطوة 2: تثبيت المكتبات
```bash
cd backend
npm install
```

⏱️ سيستغرق حوالي 1-2 دقيقة

### الخطوة 3: تشغيل Backend
```bash
npm start
```

✅ يجب أن ترى:
```
╔═══════════════════════════════════════════════════════╗
║   🚀 TiqtaQo Backend Server Running                  ║
║   📡 Server: http://localhost:3000                   ║
╚═══════════════════════════════════════════════════════╝
```

### الخطوة 4: فتح الموقع

افتح متصفحك وانتقل إلى:
- **الموقع**: `frontend/public/index.html`
- **لوحة التحكم**: `frontend/public/admin/login-updated.html`

## 🔐 بيانات تسجيل الدخول

```
البريد الإلكتروني: admin@tiqtaqo.com
كلمة المرور: Admin@123
```

⚠️ **مهم جداً**: غير كلمة المرور فوراً بعد أول تسجيل دخول!

## 🔧 تحديث الملفات القديمة

### تحديث صفحات المنتجات

في كل من الملفات التالية:
- `packs.html`
- `homme.html`
- `femme.html`
- `accessoires.html`

**ابحث عن:**
```html
<script src="js/main.js"></script>
</body>
```

**واستبدله بـ:**
```html
<script src="js/api.js"></script>
<script src="js/main-updated.js"></script>
</body>
```

### تحديث لوحة التحكم

في ملف `admin/dashboard.html`:

**ابحث عن:**
```html
<script src="admin.js"></script>
</body>
```

**واستبدله بـ:**
```html
<script src="../js/api.js"></script>
<script src="admin-updated.js"></script>
</body>
```

## 📝 اختبار النظام

### 1. اختبار Backend
افتح المتصفح واذهب إلى:
```
http://localhost:3000/api/categories
```

يجب أن ترى JSON يحتوي على الفئات.

### 2. اختبار Frontend
- افتح `index.html`
- تصفح الفئات المختلفة
- تحقق من عدم وجود أخطاء في Console (F12)

### 3. اختبار لوحة التحكم
- افتح `admin/login-updated.html`
- سجل دخول
- جرب إضافة منتج جديد
- ارفع صورة
- احفظ وتحقق من ظهور المنتج

## 🎨 إضافة منتج تجريبي

1. سجل دخول للوحة التحكم
2. اضغط "إضافة منتج"
3. املأ البيانات:
   - **الاسم**: ساعة رولكس فاخرة
   - **الوصف**: ساعة رولكس أصلية بتصميم كلاسيكي
   - **السعر**: 15000
   - **السعر القديم**: 18000
   - **الفئة**: homme
   - **Badge**: جديد
4. ارفع صورة
5. احفظ

## 🔍 حل المشاكل

### المشكلة: "Cannot GET /api/..."
**الحل**: تأكد من تشغيل Backend على المنفذ 3000

### المشكلة: المنتجات لا تظهر
**الحل**: 
1. افتح Console (F12)
2. تحقق من الأخطاء
3. تأكد من استخدام الملفات المحدثة

### المشكلة: الصور لا تظهر
**الحل**: تأكد من:
1. رفع الصورة عند إضافة المنتج
2. Backend يعمل
3. مجلد `backend/uploads` موجود

### المشكلة: لا يمكن تسجيل الدخول
**الحل**:
1. تأكد من استخدام `login-updated.html`
2. تحقق من البيانات الصحيحة
3. تأكد من تشغيل Backend

## 🌐 النشر على الإنترنت

### الخيار 1: Heroku
```bash
# تثبيت Heroku CLI
heroku login
heroku create tiqtaqo-app
git push heroku main
```

### الخيار 2: VPS (خادم خاص)
```bash
# على الخادم
git clone your-repo
cd tiqtaqo-fullstack/backend
npm install
npm install -g pm2
pm2 start server.js --name tiqtaqo
pm2 save
pm2 startup
```

### الخيار 3: Vercel / Netlify
- رفع Frontend على Vercel/Netlify
- رفع Backend على Heroku أو Railway

## 🔒 تأمين الموقع

### 1. تغيير بيانات المسؤول
في `backend/.env`:
```env
ADMIN_EMAIL=your-email@example.com
ADMIN_PASSWORD=YourStrongPassword123!
```

### 2. تغيير JWT Secret
```env
JWT_SECRET=your-very-long-random-secret-key-here
```

### 3. تفعيل HTTPS
عند النشر، استخدم شهادة SSL (Let's Encrypt مجانية)

## 📊 قاعدة البيانات

### موقع قاعدة البيانات
```
backend/database/tiqtaqo.db
```

### نسخ احتياطي
```bash
cp backend/database/tiqtaqo.db backup-$(date +%Y%m%d).db
```

### استعادة نسخة احتياطية
```bash
cp backup-20241203.db backend/database/tiqtaqo.db
```

## 🎯 الخطوات التالية

1. ✅ غير بيانات المسؤول
2. ✅ أضف منتجاتك الحقيقية
3. ✅ غير رقم WhatsApp في `main-updated.js`
4. ✅ خصص التصميم حسب رغبتك
5. ✅ اختبر على أجهزة مختلفة
6. ✅ انشر الموقع

## 📞 الدعم

إذا واجهت أي مشكلة:
1. راجع ملف `README.md`
2. راجع ملف `QUICK_START_AR.md`
3. تحقق من Console في المتصفح (F12)
4. تحقق من logs في Terminal

## 🎉 تهانينا!

لديك الآن نظام متكامل Full-Stack لإدارة متجرك الإلكتروني! 

**الفرق بين النظام القديم والجديد:**

| الميزة | القديم (localStorage) | الجديد (Full-Stack) |
|--------|---------------------|-------------------|
| حفظ البيانات | متصفح فقط | قاعدة بيانات دائمة |
| الصور | روابط خارجية | رفع على الخادم |
| الأمان | ضعيف | JWT + تشفير |
| التعدد | جهاز واحد | جميع الأجهزة |
| النشر | صعب | سهل ومرن |

---

**بالتوفيق في مشروعك! 🚀**

*تم التطوير بواسطة Suna AI* 🤖
