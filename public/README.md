# TiqtaQo - Luxury Watches & Accessories (Enhanced Version)

## 🎉 التحسينات الجديدة

### 1. ✨ أيقونات محسّنة في قسم "Nos Collections"
- **Packs**: أيقونة `fa-gifts` (هدايا) مع تدرج بنفسجي جميل
- **Homme**: أيقونة `fa-watch` (ساعة) مع تدرج أزرق أنيق
- **Femme**: أيقونة `fa-gem` (جوهرة) مع تدرج وردي راقي
- **Accessoires**: أيقونة `fa-ring` (خاتم) مع تدرج ذهبي فاخر

كل أيقونة تأتي مع:
- تدرج لوني مميز يعبر عن القسم
- تصميم جذاب وعصري
- تأثيرات حركية عند التمرير

### 2. 📸 نظام رفع الصور المباشر
في لوحة المسؤول، عند إضافة أو تعديل منتج:
- منطقة سحب وإفلات للصور
- معاينة فورية للصورة قبل الحفظ
- دعم جميع صيغ الصور (JPG, PNG, GIF)
- **بدون حد أقصى لحجم الصورة** كما طلبت
- تخزين الصور في localStorage كـ Base64

### 3. 🗂️ نظام إدارة الأقسام الكامل
لوحة تحكم جديدة لإدارة الأقسام:
- **إضافة أقسام جديدة** مع اختيار الأيقونة والترتيب
- **تعديل الأقسام** الموجودة
- **حذف الأقسام** (مع حذف المنتجات المرتبطة)
- **إخفاء/إظهار الأقسام** بدون حذفها
- **ترتيب الأقسام** حسب الأولوية

## 🚀 كيفية الاستخدام

### الصفحة الرئيسية
1. افتح `index.html` في المتصفح
2. ستظهر الأقسام بالأيقونات الجديدة الجميلة
3. الأقسام تُحمّل ديناميكياً من localStorage

### لوحة المسؤول
1. افتح `admin/login.html`
2. البيانات الافتراضية:
   - Email: `admin@tiqtaqo.ma`
   - Password: `admin123`

### إضافة منتج جديد
1. اذهب إلى تبويب "Produits"
2. اضغط "Ajouter un Produit"
3. **رفع الصورة**: اضغط على منطقة الرفع واختر صورة
4. املأ البيانات (الاسم، السعر، القسم، إلخ)
5. احفظ المنتج

### إدارة الأقسام
1. اذهب إلى تبويب "Catégories"
2. **إضافة قسم جديد**:
   - اضغط "Ajouter une Catégorie"
   - أدخل الاسم (مثل: "Montres Sport")
   - اختر أيقونة FontAwesome (مثل: `fa-running`)
   - حدد ترتيب العرض
3. **تعديل قسم**: اضغط زر "Modifier"
4. **حذف قسم**: اضغط زر "Supprimer"

## 🎨 الأيقونات المتاحة

يمكنك استخدام أي أيقونة من FontAwesome 6.4.0:
- `fa-watch` - ساعة
- `fa-gem` - جوهرة
- `fa-ring` - خاتم
- `fa-gifts` - هدايا
- `fa-crown` - تاج
- `fa-star` - نجمة
- `fa-heart` - قلب
- `fa-diamond` - ماسة
- `fa-medal` - ميدالية
- وآلاف الأيقونات الأخرى!

## 📁 هيكل الملفات

```
tiqtaqo-enhanced/
├── index.html              # الصفحة الرئيسية
├── homme.html              # صفحة منتجات الرجال
├── femme.html              # صفحة منتجات النساء
├── packs.html              # صفحة الباقات
├── accessoires.html        # صفحة الإكسسوارات
├── about.html              # صفحة من نحن
├── contact.html            # صفحة الاتصال
├── css/
│   └── style.css          # ملف الأنماط الرئيسي
├── js/
│   └── main.js            # JavaScript الرئيسي
├── admin/
│   ├── login.html         # صفحة تسجيل دخول المسؤول
│   ├── dashboard.html     # لوحة التحكم
│   └── admin.js           # JavaScript لوحة التحكم
└── images/                # مجلد الصور
```

## 🔧 المميزات التقنية

### التخزين
- جميع البيانات تُخزّن في `localStorage`
- المنتجات: `luxury_products`
- الأقسام: `luxury_categories`
- الصور: Base64 في بيانات المنتج

### الأمان
- نظام تسجيل دخول بسيط للمسؤول
- التحقق من الصلاحيات قبل الوصول للوحة التحكم

### التوافق
- متوافق مع جميع المتصفحات الحديثة
- تصميم متجاوب (Responsive)
- يعمل بدون الحاجة لخادم

## 🎯 الألوان المستخدمة

تم الحفاظ على الألوان الأصلية:
- **الذهبي**: `#D4AF37` (var(--gold))
- **الأسود**: `#1a1a1a` (var(--black))
- **الأبيض**: `#ffffff` (var(--white))
- **الرمادي الداكن**: `#2c2c2c` (var(--dark-gray))

## 📝 ملاحظات مهمة

1. **رفع الصور**: الصور تُحفظ كـ Base64 في localStorage، لذا قد يكون هناك حد للتخزين حسب المتصفح
2. **الأقسام الديناميكية**: عند إضافة قسم جديد، تأكد من إنشاء صفحة HTML له
3. **الأيقونات**: استخدم أيقونات FontAwesome بالصيغة `fa-icon-name`

## 🆕 التحديثات المستقبلية المقترحة

- إضافة قاعدة بيانات حقيقية (MySQL/MongoDB)
- نظام رفع الصور إلى خادم
- إضافة نظام البحث والفلترة
- تقارير المبيعات والإحصائيات
- نظام إدارة الطلبات

---

**تم التطوير بواسطة**: Suna AI Assistant
**التاريخ**: ديسمبر 2024
**الإصدار**: 2.0 Enhanced
